@require('../../../main')

+prefix-classes(prefix)

	.action-container
		cursor pointer
		position relative
		
		span
			cursor pointer
			position relative
			width 10px
			
			&::before
				cursor pointer
				position absolute
				top -10px
				right 5px